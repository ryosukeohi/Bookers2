<!DOCTYPE html>
<html>
  <head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <span class="d-flex align-items-center"><a class="navbar-brand p-3 nav-link text-light" href="/">Bookers</a></span>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <% if signed_in? %>
        <%= link_to 'Home', "/users/#{current_user.id}", class: 'nav-link active text-light fas fa-home' %>
          <% else %>
        <%= link_to 'Home', "/", class: 'nav-link active text-light fas fa-home' %>
           <% end %>
        </li>
        <li class="nav-item">
          <% if signed_in? %>
          <%= link_to 'Users', "/users", class: 'nav-link text-light fas fa-users' %>
          <% else %>
          <%= link_to 'About', "/home/about", class: 'nav-link text-light fas fa-link' %>
          <% end %>
        </li>
        <li class="nav-item">
          <% if signed_in? %>
          <%= link_to 'Books', "/books", class: 'nav-link text-light fas fa-book-open' %>
          <% else %>
          <%= link_to 'Sign up', "/users/sign_up", class: 'nav-link text-light fas fa-user-plus' %>
          <% end %>
        </li>
        <li class="nav-item">
          <% if signed_in? %>
          <%= link_to 'Log out', "/users/sign_out", method: :get, class: 'nav-link text-light fas fa-sign-out-alt' %>
          <% else %>
          <%= link_to 'Log in', "/users/sign_in", class: 'nav-link text-light fas fa-sign-in-alt' %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>

<%= render 'layouts/notifications' %>
    <%= yield %>
    <footer class="text-center mt-5">CopyRight Infratop.inc</footer>
  </body>
</html>