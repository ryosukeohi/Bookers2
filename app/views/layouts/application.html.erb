<!DOCTYPE html>
<html>
  <head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>Bookers2</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <span class="d-flex align-items-center"><a class="navbar-brand p-3"><%= link_to "Bookers", root_path, class: 'nav-link text-light' %></a></span>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <% if signed_in? %>
        <a class="nav-link active" aria-current="page"><%= link_to "Home", user_path(current_user.id), class: 'nav-link text-light' %></a>
          <% else %>
        <a class="nav-link active" aria-current="page"><%= link_to "Home", root_path, class: 'nav-link text-light' %></a>
           <% end %>
        </li>
        <li class="nav-item">
          <% if signed_in? %>
          <a class="nav-link text-light"><%= link_to "Users", users_path, class: 'nav-link text-light' %></a>
          <% else %>
          <a class="nav-link text-light"><%= link_to "About", about_path, class: 'nav-link text-light' %></a>
          <% end %>
        </li>
        <li class="nav-item">
          <% if signed_in? %>
          <a class="nav-link text-light" ><%= link_to "Books", books_path, class: 'nav-link text-light' %></a>
          <% else %>
          <a class="nav-link text-light"><%= link_to "Sign up", new_user_registration_path, class: 'nav-link text-light' %></a>
          <% end %>
        </li>
        <li class="nav-item">
          <% if signed_in? %>
          <a class="nav-link text-light"　data-methd="delete"><%= link_to "Log out", destroy_user_session_path, class: 'nav-link text-light' %></a>
          <% else %>
          <a class="nav-link"><%= link_to "Log in", new_user_session_path, class: 'nav-link text-light' %></a>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>

<%= render 'layouts/notifications' %>


    <%= yield %>
    <footer class="text-center mt-5">CopyRight Infratop.inc</footer>
  </body>
</html>
