<h1>Book detail</h1>

<table class="table">
<tbody>
 <tr>
   <td></td>
  <td>
   <span><%= @book.title %></span>
  </td>
  <td>
   <span><%= link_to @book.body, "/books/#{@book.id}" %></span>
  </td>
  <td><%= link_to "Edit", edit_book_path(@book.id) %></td>
  <td><%= link_to "Destroy", book_path(@book.id), method: :delete, "data-confirm" => "Are you sure?" %></td>
 </tr>
</tbody>
</table>