<div class="container">
  <div class="row">

<div class="col-md-8 col-sm-12 col-lg-5 px-5 mx-auto px-sm-0 mt-5">


<h1>Editing Book</h1>

<% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this obj from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
<% end %>

<%= form_with model:@book, local: true do |f| %>

<div class="row mb-3">
  <h4>Title</h4>
  <%= f.text_field :title %>
</div>

<div class="row mb-3">
  <h4>Option</h4>
  <%= f.text_area :body %>
</div>

  </br><%= f.submit "Update Book", class: "btn btn-success" %>

<% end %>
<%= link_to "Show", book_path(@book.id) %>
<%= link_to "Back", books_path %>

</div>
</div>
</div>